var cars = ["WRX", "Crossfire", "Saleen", "Miata"];
var numbers = [23, 12, 100, 3000000, 2];
var things = [true, false, true, false];
let explain = "Index postions describe the position of an element in an array. "
let explain2 = "For example in the cars array WRX would be in position 0"
let result = explain.concat("", explain2);
cars.push();
numbers.shift();
things.pop();

console.log(cars);
console.log(numbers);
console.log(things);
console.log(explain + explain2);
